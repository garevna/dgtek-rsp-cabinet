// import { rspWorkerEvents } from '../events'
// import { messageCallback } from './'

export function successCallback (data) {
  // window[Symbol.for('vue.instance')].$root.$emit('progress-event', false)
  // const { route, action, result } = data
  //
  // const eventName = rspWorkerEvents[route] && rspWorkerEvents[route][action] ? rspWorkerEvents[route][action] : null
  //
  // eventName && window[Symbol.for('vue.instance')].$root.$emit(eventName, result)
  //
  // if (event.data.message) messageCallback(event.data)
}
